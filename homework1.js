
// Exercise 1 - Max of Two Numbers
let number = max(5, 10)
console.log(number)

function max(a, b) {
    return (a > b) ? a : b
}    
 //   if (a > b) return a;
 //   else return b;

// firstly remove else
 //   return b;

//    (condition) ? a : // otherwise b

//    return (a > b) ? a : b // this is better code
